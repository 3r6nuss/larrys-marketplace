version: '3.8'

services:
  # PostgreSQL Database
  larrys-db:
    image: postgres:16-alpine
    container_name: larrys-db
    restart: always
    environment:
      POSTGRES_DB: larrys
      POSTGRES_USER: larrys
      POSTGRES_PASSWORD: larrys_secret
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U larrys"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Backend API
  larrys-api:
    build: ./server
    container_name: larrys-api
    restart: always
    depends_on:
      larrys-db:
        condition: service_healthy
    environment:
      DB_HOST: larrys-db
      DB_PORT: 5432
      DB_NAME: larrys
      DB_USER: larrys
      DB_PASSWORD: larrys_secret
      PORT: 3002
      CORS_ORIGIN: "*"
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:8081}
      DISCORD_BOT_TOKEN: ${DISCORD_BOT_TOKEN:-}
      DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID:-}
      DISCORD_CLIENT_SECRET: ${DISCORD_CLIENT_SECRET:-}
      DISCORD_REDIRECT_URI: ${DISCORD_REDIRECT_URI:-}
    volumes:
      - uploads:/app/uploads
    ports:
      - "3002:3002"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3002/api/health').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

  # Frontend (Nginx)
  larrys-marketplace:
    build: .
    container_name: larrys-marketplace
    depends_on:
      larrys-api:
        condition: service_healthy
    ports:
      - "8081:80"
    restart: always

volumes:
  pgdata:
  uploads:
